# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# These must be set for the application to run
# =============================================================================

# microCMS API Key (required)
# Get your API key from https://microcms.io/
MICROCMS_API_KEY=your_microcms_api_key_here

# OG Image Generation Auth Token (required)
# Used to authenticate requests to the OG image generation worker
OG_IMAGE_GEN_AUTH_TOKEN=your_og_image_gen_token_here

# =============================================================================
# OPTIONAL CLIENT-SIDE ENVIRONMENT VARIABLES
# These are exposed to the browser (must be prefixed with NEXT_PUBLIC_)
# =============================================================================

# Google Analytics 4 Measurement ID (optional, defaults to G-RV8PYHHYHN)
# Get your GA4 ID from https://analytics.google.com/
# Format: G-XXXXXXXXXX
# NEXT_PUBLIC_GA_ID=G-RV8PYHHYHN

# Hatena Star Feature Flag (optional, defaults to false)
# Set to "true" to enable Hatena Star on Japanese blog pages
# Set to "false" or leave unset to disable
# NEXT_PUBLIC_ENABLE_HATENA_STAR=false

# Sentry Browser-side DSN (optional)
# Get your DSN from https://sentry.io/settings/[organization]/projects/[project]/keys/
# Only errors from the browser will be sent to this DSN
# NEXT_PUBLIC_SENTRY_DSN=https://your-public-key@sentry.io/your-project-id

# =============================================================================
# OPTIONAL SERVER-SIDE ENVIRONMENT VARIABLES
# These are only available on the server (never exposed to browser)
# =============================================================================

# Sentry Server-side DSN (optional)
# For Cloudflare Workers error tracking
# Set this in Cloudflare Pages environment variables in production
# SENTRY_DSN=https://your-public-key@sentry.io/your-project-id

# =============================================================================
# BUILD-TIME ENVIRONMENT VARIABLES (for CI/CD)
# Only needed for automated builds with Sentry source map uploads
# =============================================================================

# Sentry CLI Auth Token (optional, for source map uploads)
# Get your token from https://sentry.io/settings/account/api/auth-tokens/
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Sentry Organization Slug (optional, for source map uploads)
# SENTRY_ORG=your-sentry-organization-slug

# Sentry Project Slug (optional, for source map uploads)
# SENTRY_PROJECT=your-sentry-project-slug

# =============================================================================
# DEVELOPMENT/TESTING VARIABLES
# =============================================================================

# Node Environment (automatically set by Next.js)
# NODE_ENV=development
